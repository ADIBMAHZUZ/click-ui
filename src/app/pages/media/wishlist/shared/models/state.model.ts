import { AllMediaFavoriteViewModel } from './view.model';

export interface WishlistStateModel {
  book: AllMediaFavoriteViewModel;
  audio: AllMediaFavoriteViewModel;
  video: AllMediaFavoriteViewModel;
}
